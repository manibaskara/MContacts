<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.centura.mcontacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_contact_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_contact_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="com.centura.mcontacts.activity.splash.SplashViewImpl">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.centura.mcontacts.activity.homeActivity.HomeViewImpl"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name="com.centura.mcontacts.activity.loginActivity.LoginViewImpl"
            android:windowSoftInputMode="stateHidden"/>
    </application>

</manifest>